{
  "name": "startup-engine",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "dev": "npm run -ws dev",
    "lint": "npm run -ws lint",
    "test": "npm run -ws test",
    "typecheck": "npm run -ws typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
